{
  "name": "Arrayy-Documentation",
  "version": "0.0.1",
  "description": "Documentation for Attayy for PHP.",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "docs:prepare": "gitbook install",
    "docs:watch": "npm run docs:prepare && gitbook serve",
    "docs:build": "npm run docs:prepare && rm -rf _book && gitbook build",
    "docs:publish": "npm run docs:build && cd _book && git init && git commit --allow-empty -m 'Update docs' && git checkout -b gh-pages && git add . && git commit -am 'Update docs' && git push git@github.com:voku/Arrayy gh-pages --force"
  },
  "author": "Lars Moelleken <lars@moelleken.org> (https://moelleken.org)",
  "license": "MIT",
  "devDependencies": {
    "gitbook-cli": "^1.0.0"
  }
}