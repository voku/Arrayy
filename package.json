{
  "name": "Arrayy-Documentation",
  "version": "0.0.1",
  "description": "Documentation for Arrayy for PHP.",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "docs:prepare": "gitbook install",
    "docs:watch": "gitbook install && gitbook serve",
    "docs:build": "gitbook install && rm -rf _book && gitbook build",
    "docs:publish": "gitbook install && rm -rf _book && gitbook build && cd _book && git init && git commit --allow-empty -m 'Update docs' && git checkout -b gh-pages && git add --all && git commit -am 'Update docs' && git push git@github.com:voku/Arrayy gh-pages --force"
  },
  "author": "Lars Moelleken <lars@moelleken.org> (https://moelleken.org)",
  "license": "MIT",
  "devDependencies": {
    "gitbook-cli": "^1.0.0"
  }
}